# Copie para .env e preencha com os valores reais (não commite segredos).
# URL base do Supabase (a mesma REST usada no n8n).
VITE_SUPABASE_URL=https://database.tnledu.shop
# Service role key — SÓ para demo local. Em produção use anon key + Auth + RLS.
VITE_SUPABASE_SERVICE_ROLE_KEY=
