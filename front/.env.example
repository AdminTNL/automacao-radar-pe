# Copie para .dev.vars e preencha para rodar localmente (wrangler dev).
# Nunca commite .dev.vars — os segredos reais ficam no Cloudflare via `wrangler secret put`.
SUPABASE_URL=https://database.tnledu.shop
SUPABASE_SERVICE_ROLE_KEY=
APP_PASSWORD=
AUTH_SECRET=
