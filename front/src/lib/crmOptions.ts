export const ENCAMINHAMENTO_OPTIONS: string[] = ['Relatório']

// Lista de opções do campo Status (radar_pe_contacts.status) — ainda sem edição
// no front; entra aqui quando o select do Status for implementado.
export const STATUS_OPTIONS: string[] = []
